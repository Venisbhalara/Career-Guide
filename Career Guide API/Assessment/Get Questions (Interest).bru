meta {
  name: Get Questions (Interest)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/assessment/questions?type=interest
  body: none
  auth: inherit
}

params:query {
  type: interest
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
